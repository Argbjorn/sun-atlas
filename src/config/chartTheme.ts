// Mirrors the Blueprint palette in index.css (--color-chart-*).
// SVG presentation attributes here don't reliably resolve CSS custom
// properties across browsers, so the values are duplicated as literals —
// keep both in sync if the theme changes.
export const CHART_GRID_COLOR = "rgba(220, 236, 255, 0.14)";
export const CHART_GRID_COLOR_FAINT = "rgba(220, 236, 255, 0.07)";
